export { C as Context, O as Options, R as ResolvedOptions, _ as default } from './index-c54d7936.js';
import 'unplugin';
import 'vite';
import 'vue/compiler-sfc';

interface VueQuery {
    vue?: boolean;
    src?: string;
    type?: 'script' | 'template' | 'style' | 'custom';
    index?: number;
    lang?: string;
    raw?: boolean;
    url?: boolean;
    scoped?: boolean;
}
declare function parseVueRequest(id: string): {
    filename: string;
    query: VueQuery;
};

export { VueQuery, parseVueRequest };
